**MUST:**

- Each skill is independent; other skills should not be mentioned unless they belong to the same group
- Custom skills must include version information, except dependencies
  ```yaml
  metadata:
    version: "2026.08.10"
  ```

- Follow @GIT.md

## Plugin validation — **Mandatory**

- Before every commit: `claude plugin validate .`
